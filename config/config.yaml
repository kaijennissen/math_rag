# LLM Configuration
llm:
  provider: "openai"
  model: "gpt-5-mini"
  temperature: 0.1

# Legacy settings (deprecated)
llm_model: "llama3.1:8b"
use_openai: false

# Application settings
docs_path: "docs"
top_k: 2
score_threshold: 0.5
